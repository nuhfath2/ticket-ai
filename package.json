{
  "name": "ticket-ai-backend",
  "version": "1.0.0",
  "description": "Support Ticket Triage Agent - Backend",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "vitest run",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "setup": "npm install && npm install --prefix client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "groq-sdk": "^0.8.0",
    "mysql2": "^3.23.1"
  },
  "keywords": [
    "ai",
    "agent",
    "support-ticket",
    "triage"
  ],
  "license": "MIT",
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
